import React from "react";

const Button = (props) => {
  return (
    <div>
      <span>변경</span>
      <span>테스트</span>
      <button>클릭</button>
        <button>테스트</button>
    </div>
  );
};

export default Button;
